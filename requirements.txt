# Runtime dependencies are kept separately so production images do not carry
# pytest, datasets, pandas and RAGAS.
-r requirements-runtime.txt

# ---- Testing ----
pytest==8.3.4
pytest-asyncio==0.24.0

# ---- Phase 2 & 3: API, Observability, Caching & Evaluation ----
langfuse>=2.20.0
ragas==0.2.15
datasets==3.2.0
pandas==2.2.3
